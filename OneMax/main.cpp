#include <iostream>
#include <vector>

using namespace std;

int main (int argc, char *argv[]){
    vector <bool> bitstring;
    int runs,iterators;
    double avgresult = 0.0;

    for(int i = 1 ; i < argc ; i++){
        char *arg = argv[i];
        switch (arg[0])
        {
        case '-':
            /* code */
            break;
        
        default:
            break;
        }
    }
    
    
    
    return 0;
}